[package]
name = "desk-app"
version = "0.1.0"
edition = "2024"

[dependencies]
dioxus = { version = "0.7.2", features = ["web"] }
desk-components = { path = "../desk-components" }
serde_json = "1.0"


[package]
name = "frappe-framework"
version = "0.1.0"
edition = "2024"

[dependencies]
rhai = { version = "1.25.1", features = ["serde", "metadata"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
